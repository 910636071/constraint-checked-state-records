# 审阅者指南

本指南面向 AI 与游戏、交互式智能体、符号评估和可复现研究 artifact 方向的审阅者。它先从一个具体的游戏智能体评估场景出发，再解释本文为什么要把问题抽象成有限符号记录和方差界。

## 一句话概括

本项目研究一个小型脚本化游戏智能体评估设置：智能体接收一串事件记录，检查器决定哪些记录可以进入已提交状态，后续行为或表达再围绕这个已提交状态进行测量。

## 具体场景

当前场景被刻意压缩到很小：

```text
脚本化事件流
  -> 候选状态记录
  -> 约束检查
  -> 已提交符号状态
  -> 后续行为或表达测量
```

问题动机是：长期运行的游戏或叙事智能体通常需要携带长期状态。如果这些状态只通过不受约束的文本摘要维护，就很难审计哪些事件真正改变了状态，以及改变的理由是什么。

本仓库把问题压缩成符号记录。它不实现完整游戏环境，也不要求审阅任何未公开系统。当前目标是先让状态更新和评估假设变得可检查，然后再决定是否进入更大的环境验证。

长期方向是评估：相比不受约束的文本摘要循环，经过检查的符号状态是否能为持久游戏智能体行为提供更稳定的基础。

## 建议先读什么

1. `README.md`：仓库边界和复现命令。
2. `paper/external_review_note.md`：当前外部评审定位。
3. `paper/paper2_finite_concept_filling.md`：Paper 2 理论白皮书草稿。

## 新贡献是什么

贡献不是方差不等式本身。真正的贡献是：把一个具体的 checked-record 评估设置，收束成一个带有可审计前提的有限有界估计器。

简单说，本项目问的是：一个规则检查后的符号状态管线，是否可以被规约为：

```text
已接受观测
  -> 有限概念坐标
  -> 显式约束检查
  -> 有界经验估计器
  -> 方差界
```

这样，后续行为一致性或表达稳定性的测量就有了一个有限、可检查的对象，而不是被留在不可控的文本摘要里。

## Paper 1 和 Paper 2 的关系

Paper 1 是冻结的公开 artifact 基座。它说明：归一化记录、轨迹、候选、有限检查和聚合评分可以在一个最小可复现管线中被分离。

Paper 2 是理论层。它利用这个分离，定义有限概念空间、验证 / 类型化 / 提交接口、有界标量和向量估计器，以及一个有条件的方差衰减陈述。

仓库里重复的实现文件是有意保留的。它们用于让理论前提可审计，而不是作为第二套软件贡献。

## 这不是什么

- 不是完整游戏环境。
- 不是产品系统。
- 不是自然语言解析器。
- 不是模型训练方法。
- 不是广泛经验优越性声明。
- 不是不受限制生成输出会收敛的声明。

## 主要审阅问题

1. 这个脚本化游戏智能体评估设置是否已经足够具体？
2. 从 checked symbolic records 到有限有界估计器的规约是否可信？
3. 方差定理的前提条件是否写得足够明确？
4. 最小 artifact 和理论层之间的关系是否清楚？
5. 下一步做受控合成评估是否是合适的验证路径？
